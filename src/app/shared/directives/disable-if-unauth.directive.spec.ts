import { DisableIfUnauthDirective } from './disable-if-unauth.directive';

describe('DisableIfUnauthDirective', () => {
  it('should create an instance', () => {
    const directive = new DisableIfUnauthDirective();
    expect(directive).toBeTruthy();
  });
});
